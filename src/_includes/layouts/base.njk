<!DOCTYPE html>
<html lang="en">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if page.url == "/" %}{{ title }}{% else %}{{ title }} — The Saiticon{% endif %}</title>
</head>
<body>
  <main>
    <h1>{{ title }}</h1>
    <p>{{ description }}</p>
    {{ content | safe }}
  </main>
</body>
</html>
